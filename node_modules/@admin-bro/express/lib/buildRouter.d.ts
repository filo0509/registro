import AdminBro from "admin-bro";
import { Router } from "express";
import { FormidableOptions } from "./types";
export declare const buildRouter: (admin: AdminBro, predefinedRouter?: Router | null | undefined, formidableOptions?: FormidableOptions | undefined) => Router;
