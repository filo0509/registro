import AdminJS from "adminjs";
import { Router } from "express";
export declare const withProtectedRoutesHandler: (router: Router, admin: AdminJS) => void;
export declare const isAdminRoute: (originalUrl: string, adminRootPath: string) => boolean;
